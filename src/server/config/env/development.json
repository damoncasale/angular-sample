{
	APP_PORT: 5000, // allows auto override by common env variable if it exists
    db: {
        FORCE_DB_SYNC: false,
        debug: false
    },
    host: "localhost",
    hostname: "localhost:5000",
    clientpath: '/../../public/',
    secret: "*)$gK5+/@wU#,|t^8['2",
    client: {
        ENV: {
            env: "development",
            urlbase: "http://localhost:5000/",
            domain: "localhost:5000"
        }
    }
}
