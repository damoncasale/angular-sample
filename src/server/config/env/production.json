{
	APP_PORT: 5000, // allows auto override by common env variable if it exists
    db: {
        FORCE_DB_SYNC: false,
        debug: false
    },
    host: "",
    hostname: "",
    clientpath: '/../../public/',
    secret: "*)$gK5+/@wU#,|t^8['2",
    client: {
        ENV: {
            env: "production",
            urlbase: "http://example.com/",
            domain: "example.com"
        }
    }
}
